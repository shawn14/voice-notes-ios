# STATUS

## 2026-08-28

- Settings/account refactor for Sign in with Apple is in the app, using the native Apple button and a more standard Account detail page.
- Hey Pocket gap pass v1 is implemented locally:
  - MCP server in `mcp/` reads the exported EEON markdown vault.
  - Folder export writes enriched note frontmatter, compiled articles, and `eeon-vault.html`.
  - Note Detail has editable speaker labels and selected-excerpt cleanup for noisy recordings.
  - Knowledge Overview has a Memory Map over compiled people/projects/topics.
  - Tasks can be shared/exported through the system share sheet for third-party handoff.
  - Ask EEON has Fast/Balanced/Thorough answer modes in Settings.
- Verified:
  - `xcodebuild -quiet -scheme "voice notes" -configuration Debug -destination "generic/platform=iOS" build`
  - `npm run build` in `mcp/`
  - `npm test` in `mcp/`
  - Installed and launched on Shawn's iPhone `F3C3794A-23F0-52DE-849D-AEB50EE743DD`.
- Release gate: `speakerLabelsJSON` is a new optional `Note` field. Before App Store submission, run a DEBUG build to register `CD_speakerLabelsJSON` via `cloudKitSchemaSeedDidRun_v6`, then deploy CloudKit schema changes to Production.
- Deferred: native OAuth task adapters and full automatic diarization. Build those only after real user demand picks the first service/provider.

## 2026-08-29

- Simplified Settings -> Connections export setup after Shawn could not find `Desktop/EEON` in the iOS Files picker.
- Superseded setup path: the app briefly used `AI Access -> Connect with iCloud Drive` as a primary `UIDocumentPickerViewController` folder-picker flow. That confused users because Files could open at `On My iPhone` with gray files. As of the later fix below, the picker is advanced/custom-folder fallback only; primary setup uses EEON's app-owned iCloud Documents vault.
- After Shawn found the Files `Open` button confusing, the app now explains before the picker that `Open` selects the folder currently shown in Files and grants EEON permission to save Markdown files there.
- Connections is now its own prominent top-level Settings section with a live status summary. Export has a visible `Disconnect Export` action that stops future markdown saves and forgets the location without deleting already exported files.
- Top-level Subscription rows now use plain `LabeledContent` for Plan/Usage so static values do not read as dead buttons; only Upgrade remains an action.
- Added iCloud Documents entitlements to debug/release so the app can write file-style exports in `iCloud.aivoiceeeon`.
- Added `NSUbiquitousContainers` metadata and bumped build number to 159 so the EEON iCloud Documents container is published as a visible iCloud Drive folder. Apple requires this Info.plist metadata for iCloud Drive visibility.
- Removed macOS-only sandbox/file/network/audio entitlements from the iOS entitlements source. Xcode's generated `voice notes.app.xcent` contains the expected app id, Apple sign-in, iCloud Documents/CloudKit, app group, push, and team entitlements. The deprecated `codesign -d --entitlements :-` form emits a misleading invalid-blob warning; `codesign -d --entitlements -` parses the signed entitlements correctly. iCloud behavior still requires on-device proof.
- Added `Test AI Access`, which writes `eeon-ai-access-test.txt` into the connected vault so a Mac AI can prove iCloud sync without reading private note bodies.
- `DocumentExportService` stores an iOS-compatible bookmark for the picker-returned security-scoped folder URL, calls `startAccessingSecurityScopedResource` before access, stops it afterward, and uses `NSFileCoordinator` for writes.
- The AI Access picker now rejects folders outside visible iCloud Drive so `On My iPhone` cannot be marked as connected for Mac/AI access. Setup confirmation now requires the proof file write to succeed; otherwise EEON disconnects and shows a folder-access error.
- Fixed a Files picker presentation bug where `Open Files` could flash and close: removed the confirmation-dialog-to-sheet handoff, stopped passing a guessed iCloud `directoryURL`, and moved the `DirectoryPicker` sheet to the stable top-level Settings view.
- Fixed the `Folder Access Failed` false-negative path: setup no longer hard-fails just because the initial security-scope call returns `false`; it saves the picker bookmark, then requires the actual proof-file write to succeed. The unconnected row now says `Choose EEON Vault in Files`, and the footer gives exact Files steps.
- Claude's local project-scoped `eeon` MCP entry points at the visible iCloud Drive path `/Users/shawncarpenter/Library/Mobile Documents/com~apple~CloudDocs/EEON Vault`.
- Verified `xcodebuild -quiet -scheme "voice notes" -configuration Debug -destination "generic/platform=iOS" build`, staged the built `.app` in `/private/tmp/eeon-install-159-apple-spec`, installed it on Shawn's iPhone `F3C3794A-23F0-52DE-849D-AEB50EE743DD`, and launched the latest folder-access fix. The new picker flow is built and installed but not yet proven by exported files appearing on the Mac.
- Historical old-state before the one-tap fix: the Mac-visible `/Users/shawncarpenter/Library/Mobile Documents/com~apple~CloudDocs/EEON Vault` folder was empty, and phone prefs still showed `documentExportFolderMode = appICloud`.
- Shawn got `Export Folder Ready`, but the Mac-visible vault was still empty. Pulling phone prefs showed the selected bookmark contained `com.apple.FileProvider.LocalStorage` under `/private/var/mobile/Containers/Shared/AppGroup/.../File Provider Storage`, meaning the user had selected `On My iPhone`, not iCloud Drive. Added explicit LocalStorage bookmark detection so phone-local folders cannot be marked ready for AI Access; rebuilt, installed, and launched on Shawn's iPhone.
- Fresh refuter pass hardened the AI Access flow: removed the unsupported guessed global-iCloud `directoryURL` (Files now restores its last valid location), balanced explicit security-scope access with `.withoutImplicitStartAccessing`, revalidates persisted bookmarks as iCloud Drive on every resolution, disconnects stale/failed connections, and enables automatic export only after the proof file writes. Coordinated content writes now use normal write coordination rather than `.forReplacing`, and directory creation uses the coordinator-returned parent URL.
- Verified the cached generic-device Debug build after the refuter fixes. The built app's signed entitlements include `CloudDocuments`, `CloudKit`, `iCloud.aivoiceeeon`, and the ubiquity container; its built Info.plist includes public document-scope metadata. Later setup removed the need for manual iCloud Drive folder selection.
- Installed the ultra-reviewed build from `/private/tmp/eeon-install-159-ultra-reviewed/voice notes.app` on Shawn's iPhone `F3C3794A-23F0-52DE-849D-AEB50EE743DD`. Remote launch was denied because the device was locked; open EEON manually after unlocking or rerun `xcrun devicectl device process launch --device F3C3794A-23F0-52DE-849D-AEB50EE743DD voice.notes.voice-notes`.
- Hey Pocket gap pass v2 closed practical local-first gaps after a fresh ultra refuter pass:
  - Home now has a visible `Ask` entry point; `AskInputSheet` has Everything / Today / 7 days / 30 days scope chips; `AnswerSheet` shows suggested follow-up buttons, keeps prior turns/sources visible, and carries date scope through short follow-ups.
  - Recording now has explicit manual Pause/Resume in `HomeRecordingOverlay`. `AudioRecorder` distinguishes manual pause from call/Siri interruption pause so interruption recovery still auto-resumes, and manual pause now stops/suppresses live transcription until Resume.
  - Tasks adds bulk visible-list actions with confirmation. Completion/reopen changes now persist back to exported markdown/frontmatter and mapped Apple Reminders.
  - Settings -> Preferences now has `People & Speakers`, a global rename surface for extracted people and saved speaker labels. Renames merge duplicate person records, update action owners/commitments/person knowledge articles, refresh transcription vocabulary, remove stale article exports, and re-export changed notes/articles.
- Refuter findings fixed: duplicate normalized-name crashes in `IntelligenceService`, display-only Ask history, non-persistent Ask scope on follow-ups, stale MCP/export state on task toggles, stale Reminders completion state, stale action/person article names after People rename, and pause starting live transcription while audio capture was paused.
- Verified v2.1 with `git diff --check` and cached generic-device Debug build: `xcodebuild -scheme "voice notes" -configuration Debug -destination "generic/platform=iOS" build` succeeded. Installed and launched the built app on Shawn's iPhone `F3C3794A-23F0-52DE-849D-AEB50EE743DD`.
- Still deliberate gaps vs Hey Pocket: remote hosted API/MCP with write-back, full automatic speaker voice-print/diarization, and first-party third-party task integrations. These are not small local UI gaps; they require explicit provider/security/product decisions.
- Locked decision from Shawn: keep EEON Apple-centric and privacy-first. Default to Sign in with Apple, CloudKit/iCloud Drive, Files folder access, Calendar, Reminders, Share Extension, and local Mac MCP over a user-owned exported vault. Do not add HeyPocket-style hosted remote MCP/API, account-wide cloud vault, OAuth fan-out, or third-party write-back without explicit product/security approval.
- Fixed the remaining AI Access/MCP setup confusion from Shawn's `On My iPhone` screenshot. Primary setup no longer opens Apple's Files picker; `Set Up AI Access` now creates/uses EEON's app-owned iCloud Documents vault, exports notes, writes `eeon-ai-access-test.txt`, then enables automatic export. The Files picker remains only as `Advanced: Choose Custom Folder`. The MCP server now falls back from known empty or placeholder-only EEON CloudDocs paths to the populated app-iCloud vault so older Claude config can still work; covered by an MCP regression test.
- Post-device check after Shawn tapped setup: Codex still could not see exported notes. Mac filesystem checks found no `eeon-ai-access-test.txt`, no `iCloud~aivoiceeeon/Documents/EEON Vault` container, and the Claude-configured visible iCloud Drive vault at `/Users/shawncarpenter/Library/Mobile Documents/com~apple~CloudDocs/EEON Vault` still returned MCP `vault_status` with `notes: 0`, `articles: 0`, `icloud_placeholders: 0`. `devicectl` app inspection timed out twice on CoreDevice, so phone-side prefs/logs were not readable from the Mac in this pass.
- Superseded the app-owned iCloud Documents setup after Shawn's screenshot showed the phone could report "Export Folder Ready" for 106 notes while the Mac still saw zero files. AI Access now treats the old `appICloud` mode as disconnected and requires the user-selected visible iCloud Drive folder that Claude is already configured to read. Installed and launched the patched Debug build on Shawn's iPhone; next proof is selecting iCloud Drive/EEON Vault in Files and watching the Mac MCP path populate.
- Shawn correctly rejected the folder-picker and same-Wi-Fi receiver UX as confusing/incomplete. Restored the Apple-centric default: Settings -> Connections -> AI Access is now one setup row, then one `AI Access` toggle plus `Disconnect AI Access`; no visible `Test AI Access`, `Export All Notes`, `Advanced`, or folder picker controls. The default setup uses EEON's app-owned iCloud Documents vault again because it is the only Apple-centric path that can work when the user is not near the Mac. Validation gate remains Mac-side: do not call it working until `~/Library/Mobile Documents/iCloud~aivoiceeeon/Documents/EEON Vault` or the MCP fallback path contains the proof file/notes.
- Verification after the simplified UI change: `npm run build`, `npm test`, `git diff --check`, and `AGENTS.md`/`CLAUDE.md` mirror checks passed. Mac-side MCP proof still fails: app-owned `iCloud~aivoiceeeon` is missing on the Mac, visible CloudDocs `EEON Vault` exists but has zero notes/articles/placeholders. Latest `xcodebuild -scheme "voice notes" -configuration Debug -destination "generic/platform=iOS" build` compiled Swift but failed at CodeSign with `internal error in Code Signing subsystem`, so this simplified build has not yet been installed on Shawn's phone.

## 2026-08-30

- Corrected the AI/MCP access diagnosis after Shawn pointed out CloudKit was already set up. The app is SwiftData + private CloudKit (`iCloud.aivoiceeeon`), and the Mac has a `cktool` management token that can run `get-teams`; that token does not authorize private user note reads.
- MCP now has a CloudKit-first source with two read paths: CloudKit Web Services when an API token + user web auth token are present, and local `xcrun cktool query-records` when using Apple developer tools/keychain locally. It maps `CD_Note` fields into the existing read-only memory tools and keeps markdown/iCloud Drive export as fallback only.
- Settings -> Connections no longer exposes the confusing folder/export flow. The visible row is `Private iCloud Sync` with On/Off status; it explains that AI tools need their own EEON CloudKit connector sign-in and that there is no iPhone folder picker in the primary path. Removed the stale visible labels: `AI Access Connected`, `Test AI Access`, `Export All Notes`, `Advanced: Choose Custom Folder`, and AI `Ready`.
- Reset the old markdown export defaults key by moving auto-export to `markdownVaultAutoExportEnabled` and clearing the legacy `documentExportEnabled` key during setup/disconnect, so Shawn's previous 106-note export state does not keep masquerading as AI access.
- Verified: MCP `npm run build`, MCP `npm test` (8/8), `git diff --check`, cached generic-device Debug `xcodebuild ... build` succeeded, installed the rebuilt app on Shawn's iPhone `F3C3794A-23F0-52DE-849D-AEB50EE743DD`, and launched it.
- Current blocker/proof: Codex cannot see EEON notes through MCP yet. `EEON_SOURCE=cloudkit EEON_CLOUDKIT_ENVIRONMENT=production npm run check:cloudkit` and the same check for `development` both return `notes: 0` with `CloudKit user-token access is not set up for cktool`. Next step is CloudKit private-user auth (`xcrun cktool save-token --type user`, `CLOUDKIT_USER_TOKEN`, or CloudKit Web Services user auth), then rerun `npm run check:cloudkit`.
- Superseded the prior iCloud Drive vault decision for AI access. Current locked architecture: the iPhone app keeps normal private iCloud/CloudKit sync; each AI workspace connects through an EEON CloudKit MCP connector with explicit Apple user auth. Files/folder export is only `Export My Data` fallback.
- Added `eeon-cloudkit-doctor` / `npm run doctor` to prove the whole connector chain without printing note bodies: MCP build, Claude config source, CloudKit management token, and private `CD_Note` read probes for Production/Development. Added `eeon-cloudkit-install-claude` / `npm run install:claude -- --apply` to rewrite Claude's EEON MCP entry from `--vault` folder mode to CloudKit mode.
- Applied the Claude config rewrite locally. `/Users/shawncarpenter/.claude.json` now points project EEON MCP at `/Users/shawncarpenter/projects/voice notes/mcp/dist/src/index.js` with `EEON_SOURCE=cloudkit` and no `--vault`; backup at `/Users/shawncarpenter/.claude.json.bak-2026-08-30T04-46-33-370Z`.
- Escalated doctor proof after the config rewrite: management token is present for teams `JM24HK4C8Q` and `BYRK5RUS4U`; Claude MCP is CloudKit mode; both Production and Development private-note probes are still blocked by missing CloudKit **user** token. `xcrun cktool save-token --type user --force` correctly opens CloudKit Console, but the generated token must be pasted into the user's own Terminal prompt, not chat.
- Removed the remaining janky Ask Library setup UI. Ask now opens as a normal pushed page with a single focused question field; there is no starter-prompt menu and no sheet-style Cancel flow for the Library entry point. Voice-routed questions and note chips still open the answer surface modally with Done.
- Tightened the Library cleanup after the refuter pass: root Library search is always visible, child Library pages do not duplicate search bars, the iPad sidebar previews 8 notes before linking to All Recent, and schema-seed filtering no longer hides legitimate names starting with "Seed project" or "Schema seed" unless they are actual `__seed` placeholders.
- Verified this UI pass with `git diff --check` and cached generic-device Debug `xcodebuild -scheme "voice notes" -configuration Debug -destination "generic/platform=iOS" build`; installed and launched the rebuilt app on Shawn's iPhone `F3C3794A-23F0-52DE-849D-AEB50EE743DD`.
- Compact home Collections after Shawn said they were too tall. The home Library surface now uses a horizontal row of compact collection chips instead of a two-column card grid, reduces the Ask Library card padding, and keeps Recent notes higher in the first viewport. Verified with `git diff --check`, rebuilt with cached generic-device Debug `xcodebuild`, then installed and launched on Shawn's iPhone.
- Removed the redundant Note detail bottom action bar after Shawn pointed out top Share/More already covered Copy, Tags, Share, and more. Note detail now uses only top-right Share plus the ellipsis menu; `Copy Note` moved into the ellipsis menu and content no longer reserves bottom-toolbar space. Verified with `git diff --check`, rebuilt with cached generic-device Debug `xcodebuild`, then installed and launched on Shawn's iPhone.
- MCP private CloudKit access is now proven after Shawn saved a CloudKit user token. Root cause of the false "can't see notes" result was `cktool query-records` argument shape: repeating `--requested-fields` returned only the final requested field, so the MCP could count records but not read titles/content. `mcp/src/cloudkit.ts` now passes one `--requested-fields` flag followed by all field names and keeps the `CD_id != __eeon_mcp_never__` filter so CloudKit does not require `recordName` to be queryable. Verified `npm run build`, `npm test` (11/11), and real MCP stdio probes: Production returned 74/74 titled notes with body lines; Development returned 100 recent notes with 100 titles and 99 body lines; `get_note` returned a 1,692-character markdown document with frontmatter/body from private CloudKit.
- Updated public positioning without naming direct competitors. Fastlane App Store metadata, the local App Store listing draft, and the public website now lead with "Apple-native AI voice memory for iPhone": no extra recorder, no pairing, no folder setup, private iCloud/CloudKit sync, Apple Calendar/Reminders follow-up, and permissioned AI workspace access through the EEON CloudKit connector. Removed the `pocket` App Store keyword and demoted markdown export to a data fallback. Website source was committed/pushed from `v0-eeon-app-design` at `1b1a650` and deployed by the repo's guarded `deploy.sh`; live `https://eeon.com` verified the new title. Verified website `pnpm build`, `pnpm exec tsc --noEmit`, metadata field lengths, no direct competitor-name matches in edited public surfaces, and `git diff --check` across voice-notes, website, and fastlane-configs. Existing website `pnpm lint` is blocked because the repo defines a lint script but does not install `eslint`.
- Added a more expressive active-capture experience. The shared `HomeRecordingOverlay` now presents a full-screen "New memory" recorder with a white swipe-down banner, animated waveform/rings, rotating private-memory / AI-ready / follow-up cues, live transcript card, and clearer bottom controls. The shared `HomeTranscribingOverlay` now shows a staged "Building your memory" pipeline for transcription, meaning extraction, follow-ups, and iCloud sync. Verified with cached generic-device Debug `xcodebuild`; installed and launched on Shawn's iPhone `F3C3794A-23F0-52DE-849D-AEB50EE743DD`.
- Pushed the next website iteration live from `v0-eeon-app-design` commit `1734d09`. The site now leads with a generated iPhone hero video (`public/eeon-hero.mp4` + poster and reproducible `scripts/make-hero-video.py`), canonical App Store links, "personal memory for your AI agents" messaging, and a new Integrations section. Guarded `deploy.sh` pushed `main` and verified `https://eeon.com` carries the expected live title. Local direct `curl` to `eeon.com` later hit a one-off DNS failure, but the deploy script's own live check had already passed.
- Locked integration messaging for the current release: EEON itself does not connect directly to Google Docs, Drive, or Gmail. The default product remains Apple-centric: iPhone capture -> private iCloud/CloudKit memory -> explicit EEON CloudKit MCP access from an AI workspace. If that AI workspace also has Google connectors, it can draft docs, summaries, and emails from EEON memory there. First-party Google write-back would require a separate OAuth/scopes/token-storage/privacy decision and should default to drafts/explicit user action, not automatic sending or broad fan-out.
- Fixed the stale website hero video after Shawn noticed it still showed the old app interface. `scripts/make-hero-video.py` now renders the current "New memory" recorder UI directly instead of compositing `app-screenshot.png`, and the site references cache-busted media names (`eeon-hero-recorder-20260830.*`) so Vercel/CDN cannot keep serving the old `/eeon-hero.mp4`. Committed/pushed website commits `b5483ac` and `162c075`; direct Vercel production deploy aliased `www.eeon.com`, and live `curl` verified both `eeon.com` and `www.eeon.com` reference the new media path while the downloaded MP4/JPEG match the generated asset sizes.
- Locked the public website headline back to "Your personal AI assistant" and added "No device to buy" only as supporting proof. The hero now uses a real simulator screenshot of the cleaned recorder UI, with cache-busted media names `eeon-hero-assistant-real-20260830.*`. Website repo `v0-eeon-app-design` is clean on `main` at commit `6048cea`; guarded deploy verified `https://eeon.com` serves the new title, canonical App Store URL, no-device copy, and live MP4/JPEG assets.
- Cleaned recorder semantics after Shawn flagged confusing controls: EEON blue remains the app/navigation/action color, while the active recording surface uses red for recording state and Finish/stop. Note Detail now offers quiz only when the note already has quiz questions, is in school-notes mode, or contains clear study signals; ordinary notes no longer default into quiz UI.
- Library collections got a compact horizontal-card treatment in both the home Library summary and the dedicated collection lists so Recent stays higher. Shawn then said to ignore the follow-up market/watchlist tab request, so no tab refactor was done. Verified the latest app edits with `git diff --check`, cached generic-device Debug `xcodebuild`, installed on Shawn's iPhone `F3C3794A-23F0-52DE-849D-AEB50EE743DD`, and launched it.
- Codex Gmail connector check: this session can access Shawn's authorized Gmail profile and label counts, but that is separate from EEON MCP. EEON itself still does not connect directly to Gmail/Google; an AI workspace can combine EEON notes and Gmail only when both connectors are independently authorized in that workspace.
- Saved the app release source to GitHub: `voice-notes-ios` main pushed at `15e1f18` (`Build EEON AI memory release`). The committed source includes the Apple-style settings/account refactor, CloudKit-first MCP, simplified Connections copy, cleaner recorder surface, conditional quiz affordance, compact collections, and note-detail action cleanup.
- Updated the public website positioning to use Shawn's "phone in your pocket" wordplay while keeping the headline "Your personal AI assistant." Website commits `d8d3dd9` and `5b802ba` were pushed/deployed; `www.eeon.com` and `eeon.com` are aliased to Vercel deployment `dpl_8JiEE2XXLQvUne2gKpZM4p7xJH8W`. Live verification: `https://www.eeon.com` serves the new metadata/copy, and `https://www.eeon.com/eeon-hero-pocket-live-20260830.mp4` returns `200 video/mp4` with `content-length: 668021`.
- Pushed App Store Connect metadata for version `3.8.0` with `scripts/asc_push.py --version 3.8.0 --skip-screenshots`; ASC readback confirmed the new description starts with "Your personal AI assistant for iPhone", promo starts with "Use the phone in your pocket", and keywords start with `personal assistant,AI notes...`. Screenshots were not replaced: the committed screenshot set is from 2026-08-26 and does not reflect the latest UI.
- App Store screenshot recapture is blocked by Fastlane snapshot tooling on this Mac: `fastlane snap app:voice-notes` repeatedly generated `-destination 'platform=iOS Simulator,name=iPhone 16 Pro Max,OS=26.3'` even though Xcode lists the installed simulator as `OS:26.3.1`, causing exit status 70 before app UI tests ran. The failed capture did not dirty or overwrite the committed screenshot set. Fastlane repo `main` pushed at `5f0b1ba` documents this runtime issue in the Voice Notes config.
- Apple binary upload/submission is still blocked by the CloudKit Production schema gate. The corrected `cloudkit_schema_sync.sh` found Production missing `CD_speakerLabelsJSON`; `APPLY=1 scripts/cloudkit_schema_sync.sh` imported the additive field into Development and verified it there. Required manual step before App Store build/submit: CloudKit Dashboard -> `iCloud.aivoiceeeon` -> Development -> Deploy Schema Changes. Then rerun `scripts/cloudkit_schema_sync.sh`; it must report "nothing to do" for Production before running `fastlane beta app:voice-notes` and `fastlane submit_review app:voice-notes version:3.8.0 build:<new build>`.

## 2026-08-31

- Fixed the public website hero fidelity after Shawn said the screenshot/buttons still looked janky. The hero generator now uses the real native recorder screenshot as the phone base and redraws only live regions: timer, waveform, and transcript. Native Minimize/Pause/Finish controls are preserved from the screenshot. Website commit `6be0120` was pushed and deployed; local `pnpm exec next build`, `pnpm exec tsc --noEmit`, and `git diff --check` passed. Live verification: `www.eeon.com` references `eeon-hero-high-fidelity-20260831.*`, and both MP4/poster return 200.
- Fixed the Ask Library composer after Shawn reported that typed prompts did not feel like they sent into chat. The composer now has a Send keyboard label, a 44pt send target, and a visible submitted-question state while Library search is loading or showing an error. Verified `git diff --check` and a cached generic-device Debug `xcodebuild`; installed and launched the build on Shawn's iPhone `F3C3794A-23F0-52DE-849D-AEB50EE743DD`. A targeted simulator UI test was blocked before app launch by Xcode's UI-test runner (`NSMachErrorDomain Code=-308`), so final Ask Library tap proof remains manual/on-device.
